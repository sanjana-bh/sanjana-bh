name: Generate Snake Contribution Graph

on:
  schedule:
    - cron: "0 0 * * *"  # Runs daily at midnight UTC
  workflow_dispatch:      # Allows manual trigger
  push:
    branches:
      - main              # Also runs when you push to main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Generate Snake Contribution Graph
        uses: platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            assets/snake-contribution-graph-dark.svg?color_snake=C9788E&color_dots=1E293B,C9788E,D49AAA,E3A8B7
          
      - name: Commit and push generated SVGs
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Add and commit the generated files
          git add assets/snake-contribution-graph-dark.svg
          git diff --staged --quiet || git commit -m "🐍 Update snake contribution graph [automated]"
          git push
